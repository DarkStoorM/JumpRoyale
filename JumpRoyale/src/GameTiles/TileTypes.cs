namespace JumpRoyale;

public enum TileTypes
{
    Concrete,
    Gold,

    /// <summary>
    /// This is the default tile that is being used for drawing. You don't need to explicitly use this tile.
    /// </summary>
    Stone,
}
